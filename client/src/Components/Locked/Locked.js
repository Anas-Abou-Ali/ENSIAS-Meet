import "./Locked.scss";

const Locked = (props) => {
  return (
    <div className="lockedContainer">
      To use ENSIAS Meet you have to enable your camera and microphone.
    </div>
  );
};

export default Locked;
